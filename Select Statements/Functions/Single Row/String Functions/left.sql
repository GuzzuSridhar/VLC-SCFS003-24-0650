/* Left Function 
syntax
LEFT(string, length)
string: The string from which to extract characters.
length: The number of characters to extract from the left side of the string.
*/

select left('Hello World', 5);

select left(ename, 1) from emp.emp;


